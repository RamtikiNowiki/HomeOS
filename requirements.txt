# Direct dependencies (top-level). For exact reproducible installs use requirements-lock.txt.
# Regenerate lock: pip install -r requirements.txt && pip freeze > requirements-lock.txt

Flask==3.1.0
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
SQLAlchemy==2.0.36
psycopg[binary]==3.3.4
gunicorn==23.0.0
python-dotenv==1.0.1
